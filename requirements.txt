# NativaCare Chatbot — Python dependencies
# Install with:  pip install -r requirements.txt
# Tested against Python 3.10+.

# ---------- Web framework ----------
fastapi==0.115.0
uvicorn[standard]==0.32.0

# ---------- Data + validation ----------
pydantic==2.9.2
python-dotenv==1.0.1

# ---------- HTTP clients ----------
httpx==0.27.2

# ---------- LLM (Groq uses the OpenAI-compatible SDK) ----------
openai==1.54.0

# ---------- Date parsing ----------
dateparser==1.2.0

# ---------- Google Calendar (only needed if GOOGLE_CALENDAR_ID is set) ----------
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-httplib2==0.2.0
