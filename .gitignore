# Secrets — NEVER commit these
.env
.env.local
.env.production
service_account.json
[Ss]ervice_account.json
*-credentials.json
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
venv/
env/
.venv/
.env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local dev artifacts
*.log
local_*
scratch_*
test_output/

# Crawler state file (regenerated on each run)
.crawler_state.json
crawl.log

# App data — invoices, appointments, payment proofs, chat sessions.
# Contains real patient names/phones/emails/payment screenshots once
# live. Never commit this — see NATIVACARE_DB_PATH / UPLOADS_DIR in .env
# if you've customized either path beyond the defaults below.
*.db
*.db-journal
/uploads/
